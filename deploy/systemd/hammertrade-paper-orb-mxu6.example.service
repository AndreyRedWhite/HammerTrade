# HammerTrade Paper Trader — MXU6 ORB SHORT (no real orders, READONLY_TOKEN only).
#
# Install:
#   sudo cp deploy/systemd/hammertrade-paper-orb-mxu6.example.service /etc/systemd/system/hammertrade-paper-orb-mxu6.service
#   sudo systemctl daemon-reload && sudo systemctl enable --now hammertrade-paper-orb-mxu6

[Unit]
Description=HammerTrade Paper Trader — MXU6 ORB SHORT (paper only, no real orders)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/hammertrade
EnvironmentFile=/opt/hammertrade/.env
ExecStart=/opt/hammertrade/.venv/bin/python scripts/run_orb_paper_trader.py \
    --ticker MXU6 \
    --class-code SPBFUT \
    --timeframe 1m \
    --direction SHORT \
    --point-value-rub 1.0 \
    --opening-range-start 10:00 \
    --opening-range-end 11:00 \
    --take-r 2.0 \
    --state-db data/paper/paper_state_mxu6_orb_short.sqlite \
    --status-file runtime/paper_status_MXU6_ORB_SHORT.json \
    --csv-output out/paper/paper_trades_MXU6_ORB_SHORT.csv \
    --log-file logs/paper_MXU6_ORB_SHORT.log \
    --experiment-name orb_mxu6_short \
    --env prod \
    --market-hours-config configs/market_hours/moex_futures.yaml \
    --api-timeout-sec 10
Restart=always
RestartSec=10
User=vorontsov

[Install]
WantedBy=multi-user.target
